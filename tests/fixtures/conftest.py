"""Fixture path helpers for test data files.

HDF5, MCAP, and other test fixture path helpers are defined in the top-level
``tests/conftest.py`` so they are visible to all test subdirectories (unit,
integration, etc.). This file exists as the originally planned location but
the fixtures were promoted to the top-level conftest for broader visibility.
"""
